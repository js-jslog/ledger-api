# Probes use a dedicated database so a dirty carried-over volume in `ledger`
# cannot be mistaken for a code problem, and so nothing else is destroyed.
DATABASE_URL=postgres://ledger:ledger@localhost:55432/ledger_probe
